---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: testruns.etos.eiffel-community.github.io
spec:
  group: etos.eiffel-community.github.io
  names:
    kind: TestRun
    listKind: TestRunList
    plural: testruns
    singular: testrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=="Environment")].reason
      name: Environment
      type: string
    - jsonPath: .status.conditions[?(@.type=="SuiteRunner")].reason
      name: Suiterunner
      type: string
    - jsonPath: .status.conditions[?(@.type=="Active")].status
      name: Active
      type: string
    - jsonPath: .status.verdict
      name: Verdict
      type: string
    - jsonPath: .status.conditions[?(@.type=="Active")].message
      name: Message
      type: string
    - jsonPath: .metadata.labels.etos\.eiffel-community\.github\.io/id
      name: ID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TestRun is the Schema for the testruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TestRun
            properties:
              artifact:
                description: Artifact is the ID of the software under test. The ID
                  is a UUID, any version, and regex matches that.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
              cluster:
                description: Name of the ETOS cluster to execute the testrun in.
                type: string
              deadline:
                description: |-
                  Deadline is the timestamp, in unix epoch seconds, by which the testrun
                  must have completed. If the deadline is exceeded, the controller will fail
                  the testrun. If not set, it is computed from Timeout at the start of the testrun.
                  If both Timeout and Deadline are set, Deadline takes precedence.
                format: int64
                type: integer
              environmentProvider:
                properties:
                  image:
                    description: Image describes the docker image to run for a service.
                      ETOS applies defaults if empty.
                    type: string
                  imagePullPolicy:
                    description: |-
                      ImagePullPolicy describes the pull policy to use for the image. ETOS applies PullIfNotPresent
                      if empty.
                    type: string
                type: object
              id:
                description: ID is the test suite ID for this execution. Will be generated
                  if nil. The ID is a UUID, any version, and regex matches that.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
              identity:
                type: string
              logListener:
                properties:
                  image:
                    description: Image describes the docker image to run for a service.
                      ETOS applies defaults if empty.
                    type: string
                  imagePullPolicy:
                    description: |-
                      ImagePullPolicy describes the pull policy to use for the image. ETOS applies PullIfNotPresent
                      if empty.
                    type: string
                type: object
              providers:
                description: |-
                  Providers to use for test execution. These names must correspond to existing
                  Provider kinds in the namespace where a testrun is created.
                properties:
                  executionSpace:
                    type: string
                  iut:
                    type: string
                  logArea:
                    type: string
                required:
                - executionSpace
                - iut
                - logArea
                type: object
              retention:
                description: Retention describes the failure and success retentions
                  for testruns.
                properties:
                  failure:
                    type: string
                  success:
                    type: string
                type: object
              suiteRunner:
                properties:
                  image:
                    description: Image describes the docker image to run for a service.
                      ETOS applies defaults if empty.
                    type: string
                  imagePullPolicy:
                    description: |-
                      ImagePullPolicy describes the pull policy to use for the image. ETOS applies PullIfNotPresent
                      if empty.
                    type: string
                type: object
              suiteSource:
                description: |-
                  SuiteSource is the URL from which the test suite definition can be fetched.
                  It is used to set batchesUri in the TERCC event.
                type: string
              suites:
                items:
                  description: Suite to execute.
                  properties:
                    dataset:
                      description: Dataset for this suite.
                      x-kubernetes-preserve-unknown-fields: true
                    name:
                      description: Name of the test suite.
                      type: string
                    priority:
                      default: 1
                      description: Priority to execute the test suite.
                      type: integer
                    tests:
                      description: Tests to execute as part of this suite.
                      items:
                        properties:
                          environment:
                            description: TestEnvironment to run tests within.
                            type: object
                          execution:
                            description: Execution describes how to execute a testCase.
                            properties:
                              checkout:
                                items:
                                  type: string
                                type: array
                              command:
                                type: string
                              environment:
                                additionalProperties:
                                  type: string
                                type: object
                              execute:
                                items:
                                  type: string
                                type: array
                              parameters:
                                additionalProperties:
                                  type: string
                                type: object
                              testRunner:
                                type: string
                            required:
                            - checkout
                            - command
                            - environment
                            - parameters
                            - testRunner
                            type: object
                          id:
                            type: string
                          testCase:
                            description: TestCase metadata.
                            properties:
                              id:
                                type: string
                              tracker:
                                type: string
                              uri:
                                type: string
                              version:
                                type: string
                            required:
                            - id
                            type: object
                        required:
                        - environment
                        - execution
                        - id
                        - testCase
                        type: object
                      type: array
                  required:
                  - dataset
                  - name
                  - priority
                  - tests
                  type: object
                type: array
              testRunner:
                properties:
                  version:
                    type: string
                required:
                - version
                type: object
              timeout:
                default: 86400
                description: |-
                  Timeout is the maximum duration, in seconds, the testrun is allowed to take.
                  If not set, defaults to 86400 (24 hours). If both Timeout and Deadline are set,
                  Deadline takes precedence.
                format: int64
                type: integer
            required:
            - artifact
            - identity
            - providers
            - suites
            type: object
          status:
            description: status defines the observed state of TestRun
            properties:
              completionTime:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              environmentRequests:
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              startTime:
                format: date-time
                type: string
              suiteRunners:
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              verdict:
                type: string
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.cluster
    - jsonPath: .spec.artifact
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=="Environment")].reason
      name: Environment
      type: string
    - jsonPath: .status.conditions[?(@.type=="SuiteRunner")].reason
      name: Suiterunner
      type: string
    - jsonPath: .status.conditions[?(@.type=="Active")].status
      name: Active
      type: string
    - jsonPath: .status.verdict
      name: Verdict
      type: string
    - jsonPath: .status.conditions[?(@.type=="Active")].message
      name: Message
      type: string
    - jsonPath: .metadata.labels.etos\.eiffel-community\.github\.io/id
      name: ID
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TestRun is the Schema for the testruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TestRun
            properties:
              artifact:
                description: Artifact defines the unique identifier for the artifact
                  under test as UUID. This field is required.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
              cluster:
                description: |-
                  Cluster defines the cluster to run the test suite on.
                  This field is optional and can be used to specify a particular cluster for test execution.
                type: string
              deadline:
                description: |-
                  Deadline defines the timestamp, in unix epoch seconds, by which the testrun
                  must have completed. If the deadline is exceeded, the controller will fail
                  the testrun. If not set, it is computed from Timeout at the start of the testrun.
                  If both Timeout and Deadline are set, Deadline takes precedence.
                format: int64
                type: integer
              environmentProvider:
                description: |-
                  EnvironmentProvider defines the image to use for the environment provider.
                  If not specified, a default image will be used.
                properties:
                  image:
                    description: Image defines the docker image to run for a service.
                      ETOS applies defaults if empty.
                    type: string
                  imagePullPolicy:
                    description: |-
                      ImagePullPolicy defines the pull policy to use for the image. ETOS applies PullIfNotPresent
                      if empty.
                    type: string
                type: object
              id:
                description: ID defines the unique identifier for this test run as
                  UUID. This field will be generated if not set.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
              identity:
                description: Identity defines the identity of the artifact as a packageurl.
                minLength: 1
                pattern: ^pkg:[a-z]+/.+$
                type: string
              name:
                description: Name defines the name of the suite definition.
                maxLength: 255
                minLength: 1
                type: string
              providers:
                description: |-
                  Providers defines the providers to use for test execution. These names must correspond to existing
                  Provider kinds in the namespace where a testrun is created.
                  If not set the controller will attempt to find the default providers in the namespace.
                  If any of the default providers are not found, the controller will fail the testrun.
                properties:
                  executionSpace:
                    description: ExecutionSpace defines the provider to use for execution
                      space provisioning.
                    type: string
                  iut:
                    description: IUT defines the provider to use for item under test
                      provisioning.
                    type: string
                  logArea:
                    description: LogArea defines the provider to use for log area
                      provisioning.
                    type: string
                type: object
              retention:
                description: |-
                  Retention defines the failure and success retentions for keeping testrun resources after completion.
                  If not set, the testrun resource will not be deleted after completion.
                properties:
                  failure:
                    description: Failure defines the duration to retain testrun resources
                      after a failed execution.
                    type: string
                  success:
                    description: Success defines the duration to retain testrun resources
                      after a successful execution.
                    type: string
                type: object
              schemaVersion:
                default: v1beta1
                description: |-
                  SchemaVersion defines the version of the test run schema.
                  This field is used to determine which version of the test run schema to use when creating a test run.
                enum:
                - v1beta1
                minLength: 1
                type: string
              suiteRunner:
                description: |-
                  SuiteRunner defines the image to use for the suite runner.
                  If not specified, a default image will be used.
                properties:
                  image:
                    description: Image defines the docker image to run for a service.
                      ETOS applies defaults if empty.
                    type: string
                  imagePullPolicy:
                    description: |-
                      ImagePullPolicy defines the pull policy to use for the image. ETOS applies PullIfNotPresent
                      if empty.
                    type: string
                type: object
              suiteSource:
                description: |-
                  SuiteSource defines the URL from which the test suite definition can be fetched.
                  It is used to set batchesUri in the TERCC event.
                type: string
              suites:
                description: Suites defines the list of test suites to run.
                items:
                  description: Suite defines a single test suite contining prioritized
                    test executions.
                  properties:
                    dataset:
                      description: |-
                        Dataset defines an optional dataset to be used for the test suite. The content and structure of the dataset is
                        provider-defined and can be used to pass arbitrary data to the test execution environment. The dataset is
                        represented as a raw JSON object, allowing for flexible and extensible data structures as needed by different
                        providers and test cases.
                      x-kubernetes-preserve-unknown-fields: true
                    priority:
                      default: 1
                      description: |-
                        Priority defines the execution priority when multiple suites are defined.
                        Lower values indicate higher priority. Suites with the same priority may be executed in any order.
                      minimum: 1
                      type: integer
                    testExecutions:
                      description: TestExecutions defines the list of test executions
                        to run in this suite.
                      items:
                        description: |-
                          TestExecution defines a single test execution combining a test case, its execution instructions,
                          and environment requirements.
                        properties:
                          dependencies:
                            description: Dependencies defines an optional list of
                              test execution IDs that must complete before this test
                              can run.
                            items:
                              pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          environment:
                            description: |-
                              Environment defines configuration for a test execution, including the test runner image, environment variables,
                              and additional provider-defined resources.
                            properties:
                              additionalResources:
                                description: |-
                                  AdditionalResources defines provider-defined resources required by the test beyond the primary IUT (Item Under
                                  Test), e.g. a sidecar containers or additional hardware. Each items fields are open-ended and interpreted by
                                  the provider responsible for that resource; not every field needs to be defined in this schema.
                                items:
                                  description: |-
                                    AdditionalResource defines a single provider-defined resource required by a test, such as a sidecar container or
                                    additional hardware. Aside from the 'type' discriminator, fields are open-ended and validated by the provider
                                    that handles the resource, not by this schema.
                                  properties:
                                    type:
                                      description: |-
                                        Type defines a provider-defined resource type used to select the provider that handles this resource, e.g.
                                        'device' or 'container'. The set of valid values is defined by the available providers, not by this schema.
                                      type: string
                                  required:
                                  - type
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                type: array
                              environmentVariables:
                                additionalProperties:
                                  type: string
                                description: EnvironmentVariables defines keyt-value
                                  pairs of environment variables to set in the test
                                  runner.
                                type: object
                              testRunner:
                                description: |-
                                  TestRunner defines the container image to use as the test runner, e.g.
                                  'ghcr.io/eiffel-community/etos-base-test-runner:latest'.
                                type: string
                            required:
                            - testRunner
                            type: object
                          execution:
                            description: Execution defines instructions for how to
                              execute a test case.
                            properties:
                              checkout:
                                description: Checkout defines shell commands to check
                                  out the source code. Executed in order before the
                                  test command.
                                items:
                                  type: string
                                type: array
                              command:
                                description: Command defines the shell command to
                                  execute the test, e.g. a pytest invocation.
                                type: string
                              preExecution:
                                description: |-
                                  PreExecution defines shell commands to run after checkout but before the main test command
                                  (e.g. setup steps, dependency installation).
                                items:
                                  type: string
                                type: array
                            required:
                            - command
                            type: object
                          id:
                            description: ID defines the unique identifier for this
                              test execution as UUID.
                            minLength: 1
                            pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                            type: string
                          testCase:
                            description: |-
                              TestCase defines metadata about a test case. The repository and version can be used to check out the test
                              source code.
                            properties:
                              id:
                                description: |-
                                  ID defines the fully qualified test case identifier, e.g
                                  'tests/integration/test_api.py::TestAPI::test_create_resource'
                                minLength: 1
                                type: string
                              repository:
                                description: Repository defines the repository URL
                                  where the test case source code can be found, e.g.
                                  a Git repository URL.
                                type: string
                              uri:
                                description: URI defines an optional URI to the test
                                  case documentation or external reference for a test
                                  case.
                                type: string
                              version:
                                description: Version defines the version of the test
                                  case, typically a Git commit SHA or branch name.
                                type: string
                            required:
                            - id
                            type: object
                        required:
                        - environment
                        - execution
                        - id
                        - testCase
                        type: object
                      minItems: 1
                      type: array
                  required:
                  - testExecutions
                  type: object
                minItems: 1
                type: array
              testRunner:
                description: |-
                  TestRunner defines the test runner version to use for executing tests.
                  If not specified, a default test runner version will be used.
                properties:
                  version:
                    type: string
                required:
                - version
                type: object
              timeout:
                default: 86400
                description: |-
                  Timeout defines the maximum duration, in seconds, the testrun is allowed to take.
                  If not set, defaults to 86400 (24 hours). If both Timeout and Deadline are set,
                  Deadline takes precedence.
                format: int64
                type: integer
            required:
            - artifact
            - identity
            - name
            - suites
            type: object
          status:
            description: status defines the observed state of TestRun
            properties:
              completionTime:
                format: date-time
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the TestRun resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              environmentRequests:
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              verdict:
                type: string
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.cluster
    - jsonPath: .spec.artifact
    - jsonPath: .spec.id
    served: true
    storage: false
    subresources:
      status: {}
