apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etos
  labels:
    app.kubernetes.io/name: etos
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: "etos.etos-staging.k8s.axis.com"
      http:
        paths:
          - path: /(environment-provider)/?(.*)
            pathType: Prefix
            backend:
              service:
                name: etos-environment-provider
                port:
                  number: 80
          - path: /(api)/?(.*)
            pathType: Prefix
            backend:
              service:
                name: etos-api
                port:
                  number: 80
